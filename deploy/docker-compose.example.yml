services:
  inspiration-catcher:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.example
    restart: unless-stopped
    env_file:
      - ../.env
    environment:
      HOST: 0.0.0.0
    ports:
      - "${APP_PORT:-4173}:4173"
    volumes:
      - ${DATA_DIR:-./data}:/app/data
