# Inspiration Catcher 项目交接文档

> 更新时间：2026-07-28  
> 用途：新对话或新开发者接手时，先阅读本文和项目代码，再继续开发。  
> 线上地址：https://inspiration.zzhhh.site

## 1. 核心目标与成功标准

这是一个“视频灵感捕捉与知识整理”网页。用户刷到有启发的视频后，可以快速粘贴链接，系统自动解析视频、持久保存封面和来源信息、转写为简体中文，并把转写、个人感想、阅读标注、AI 拆解、类似案例和标签统一保存为一条灵感。

成功标准：

- 支持微信视频号、抖音、B站视频解析。
- 自动保存标题、来源、原链接和本地持久化封面，不能依赖会失效的临时图片 URL。
- Whisper Medium 在服务器侧转写，并显示可恢复的实时进度。
- 用户可阅读、编辑、高亮、下划线和批注转写正文。
- AI 输出精炼、信息密度高，并永久绑定到对应灵感。
- PC 和手机都能顺畅完成解析、阅读、编辑、标签和归档操作。
- 本站是事实源，飞书是异步镜像；飞书故障不能阻塞本站保存。
- 数据库和接口始终按多用户结构设计，当前先上线个人组织版本。

## 2. 关键产品模型与约束

### 灵感库

- 每条灵感只属于一个灵感库。
- 每位用户有一个不可删除的默认库“待分类”，但允许重命名。
- 用户可创建、重命名、删除灵感库，也可移动笔记。
- 删除自定义灵感库时，该库笔记转移到默认库，不删除笔记。
- 主页保存灵感时，可以通过悬浮入口选择或新建灵感库。
- 灵感库负责唯一归档，不能与标签组混为一体。

### 标签

- 每条灵感可以有多个标签。
- 标签最多两级：分组和组内标签，没有第三级。
- 标签可搜索、创建、移动到其他分组、删除和管理。
- AI 推荐不超过 10 个，先匹配已有分组和已有标签；确实没有贴切项时才建议新标签或新分组。
- 标签要可长期复用，不能只复述单篇内容中的临时措辞。
- AI 推荐只是候选，用户未选择的标签不得进入标签系统。

### AI 与内容

- 支持 DeepSeek、OpenAI、Claude、Grok。
- 每个平台支持多套第三方 URL 与 API Key 配置，并可下拉切换、增加和删除配置。
- 模型列表与思考强度联动；不支持思考强度的模型不显示该设置，默认强度为中等。
- AI 高密度正文、视频拆解、类似案例和推荐标签都需要专用提示词。
- AI 调用记录可以删除，但成功输出必须作为正文保留并绑定单条灵感。
- AI 失败应显示 HTTP 状态、错误类型、服务端信息，并允许手动重试；失败状态不能覆盖此前已经成功的正文。
- 视频拆解显示在类似案例前；AI 实验室用于生成和并排阅读，不是跨灵感堆放结果的仓库。

### 阅读与交互

- 转写统一输出简体中文，并经过适合阅读的排版处理。
- 阅读区使用明亮舒适的正文样式，不使用黑色背景。
- 支持高亮、下划线、用户批注、AI 批注和一键复制。
- 手机端长按选中文字后，高亮、下划线等操作都必须即时可见。
- 封面保持原始比例，主页和“我的灵感”中不能裁掉关键画面。
- “快速记录”“我的灵感”“最近捕捉”“标签管理”“AI 实验室”是独立功能视图，不应把所有内容塞在主页。

## 3. 技术架构与环境

```text
本地项目：
D:\Data\01_Projects\2026\explore\inspiration-catcher

线上服务器登录：
wsl -d Ubuntu-24.04 -- ssh cloud-op

线上目录：
/home/op/apps/inspiration-catcher

服务重启：
systemctl --user restart inspiration-catcher.service

线上数据库：
/home/op/apps/inspiration-catcher/data/inspiration.sqlite3
```

技术栈：

- Node.js 单体服务
- SQLite / better-sqlite3，当前 schema v4
- 原生 HTML、CSS、JavaScript
- Vite 构建
- TipTap 阅读编辑器
- 飞书 OAuth、Wiki、Docx、Bitable、Drive API
- Gaia worker 上的 Whisper Medium

安全约束：不要在对话、提交、文档或日志中复述飞书 App Secret、AI API Key、用户密码。已有密钥只从服务器环境变量或受保护配置读取。

## 4. 关键文件与代码职责

- `server.mjs`：认证、视频处理、灵感库/标签/笔记 API、转写队列和恢复。
- `lib/migrations.mjs`：数据库迁移，当前 schema v4。
- `lib/feishu-integration.mjs`：飞书 OAuth、异步队列、Wiki 目录、Docx、Bitable 和图片同步。
- `app.js`：前端全局状态、导航、灵感库、标签、飞书和笔记交互。
- `index.html`：PC/手机布局、视觉样式和主要页面结构。
- `src/workspace.js`：转写阅读、编辑、选区、高亮、下划线和批注。
- `tests/feishu-integration.test.mjs`：飞书原生块、目录移动、重命名和清理测试。
- `tests/feishu-migration.test.mjs`：schema v4 迁移测试。
- `tests/feishu-api.test.mjs`：多用户灵感库与接口测试。
- `tests/library_e2e.py`：本地 PC/手机完整交互测试。
- `tests/library_online_check.py`：线上只读界面检查。
- `docs/plans/2026-07-26-transcript-annotation-workspace-design.md`：阅读标注工作区设计。
- `docs/plans/2026-07-27-feishu-personal-org-sync-design.md`：飞书个人组织同步设计。
- `docs/plans/2026-07-28-inspiration-libraries-design.md`：灵感库设计和数据约束。
- `启动本地版.bat`、`关闭本地版.bat`、`打开线上版.bat`：Windows 便捷入口。

## 5. 已完成功能

- 注册登录和多用户数据隔离。
- 微信视频号、抖音、B站视频解析。
- 标题、来源、原链接和封面持久化。
- 转写队列、进度显示、刷新恢复和过时中间文件清理。
- Whisper 转写简体中文、AI 排版、高密度阅读模式和富文本编辑。
- 高亮、下划线、用户批注、AI 批注和一键复制。
- AI 拆解、类似案例和单条灵感绑定。
- 标签分组、搜索、移动、删除、手动添加和 AI 推荐。
- DeepSeek、OpenAI、Claude、Grok 多平台、多服务配置管理。
- 灵感库创建、重命名、删除、筛选和笔记移动。
- 主页保存时选择灵感库。
- 飞书 OAuth、知识空间选择和异步自动同步。
- 飞书富文本、列表、引用、任务项、链接和封面。
- 飞书目录按灵感库组织，多维表格包含“灵感库”字段。
- 飞书文档保留不被本站覆盖的“我的飞书补充”。

## 6. 重要故障、原因与修复

### 视频解析出现 `Failed to fetch`

微信视频号外部链接不能直接访问，需要通过元宝登录态和 Cookie 解析真实媒体地址。系统采用微信分享链接到元宝解析，再下载视频的链路。后续排查时要区分浏览器跨域、服务端解析失败和元宝 Cookie 过期。

### AI 返回 HTML、502、503 或 504

- `PROVIDER_INVALID_RESPONSE`：第三方 URL 指向网页或控制台，而不是兼容 API 地址，返回了 HTML。
- `PROVIDER_ERROR`：上游返回 503，服务暂不可用。
- `PROVIDER_TIMEOUT` / 504：上游连续无响应或反向代理超时。
- 修复原则：保留具体状态码和上游响应，允许切换服务配置后手动重试；不要把失败结果覆盖到已成功正文。

### 飞书 OAuth 回调白屏

回调页需要输出可读的成功/失败界面，并将连接状态同步回网页，而不是直接显示空白接口响应。

### 飞书出现 `FEISHU_API_ERROR_[object Object] · InvalidFilter`

错误对象未序列化且筛选参数格式不符合飞书接口。已改进错误展开与查询逻辑，真实同步验证通过。

### 飞书图片一直缓冲后上传失败

原因是 Docx 图片上传链路缺少文档路由信息和图片块替换步骤。正确流程：

1. 创建空图片块并取得 `IMAGE_BLOCK_ID`。
2. 调用 `drive/v1/medias/upload_all`。
3. 上传参数包含：

```json
{
  "parent_type": "docx_image",
  "parent_node": "IMAGE_BLOCK_ID",
  "extra": "{\"drive_route_token\":\"DOCUMENT_ID\"}"
}
```

4. 调用 Docx block PATCH：

```json
{
  "replace_image": {
    "token": "FILE_TOKEN",
    "width": 540,
    "height": 720
  }
}
```

线上两张封面已经验证 token 存在，尺寸为 `540x720`。

### 飞书文档排版和目录体验差

已完成：

- Markdown 转飞书原生块。
- 删除“暂无内容”空章节。
- 隐藏 `key/doubt/action` 内部字段。
- AI 批注也使用 Markdown 转换。
- AI 小标题使用加粗段落，避免目录过长。
- 原始转写移动到文末。
- 首屏优先展示来源、标签、核心启发和封面。
- 飞书目录以灵感库为父节点组织。
- 移动灵感时移动原 Wiki 节点，不重建文档，以保留 URL、评论和“我的飞书补充”。

## 7. 用户历次纠正形成的长期规则

- 线上地址固定为 `https://inspiration.zzhhh.site`，不要使用旧路径站点。
- 首页只服务快速记录：视频解析在上，转写阅读紧随其后；不要左右两列拉出大块空白。
- 最近捕捉、标签管理、AI 高密度正文不要堆到首页。
- 最近捕捉是独立模块，不属于标签管理。
- 标签管理默认展示所有分组，组内标签点击展开后再显示。
- 解析完成后即使用户离开页面，标题、封面和转写也不能丢失。
- 保存灵感时，所有视频元数据、正文、感想、标注、AI 拆解和案例必须归属于同一条灵感。
- 灵感详情必须能完整向下滚动查看笔记、感想、拆解和类似案例。
- AI 推荐标签要理解完整转写并推荐可复用概念；不要机械禁止“成长”等词，而要根据语境判断是否贴切。
- 已有标签优先，但不强行套用；现有体系不合适时才推荐新标签或新分组。
- 标签是交叉分类，灵感库是唯一归档；最终确认每条灵感只属于一个灵感库。
- 飞书同步必须按灵感库组织子目录，便于筛选和查找。
- 中文统一使用简体。
- UI 要专业、清晰、克制、信息密度高，同时适配 PC 和手机。

## 8. 当前状态与验证记录

最近一次已知验证结果：

```text
本地全量测试：55/55
服务器专项测试：16/16
线上飞书同步：成功 3，失败 0
线上健康状态：ok
AI 配置：4/4
服务状态：active
```

线上三条已有灵感都已分配到默认灵感库，并同步到同一个飞书“待分类”目录；文档 URL 和 ID 未改变。

最近生产备份：

```text
/home/op/apps/inspiration-catcher/backups/inspiration-libraries-20260728-155629
```

注意：以上是交接时的最后已知状态。新对话开始后，应先只读检查当前线上状态，不要把该记录当成永久实时状态。

## 9. 下一轮如何继续

当前没有尚未完成的明确需求。下一轮收到新需求后：

1. 先阅读本文、相关设计文档和涉及的代码，不要重新实现已有功能。
2. 只读检查线上页面、服务状态和最新数据表现。
3. 修改前备份服务器代码与 SQLite 数据库。
4. 保持 schema v4、多用户隔离和“每条灵感只属于一个灵感库”的约束。
5. 涉及飞书时先跑 mock 测试，再真实同步一条测试笔记验证。
6. 完成后运行：

```powershell
npm test
npm run build
```

7. 部署后检查：

```bash
systemctl --user is-active inspiration-catcher.service
```

8. 用 PC 和手机视口验证实际交互，不能只看接口测试。

## 新对话启动提示词

```text
请先完整阅读 D:\Data\01_Projects\2026\explore\inspiration-catcher\PROJECT_HANDOFF.md，并检查 D:\Data\01_Projects\2026\explore\inspiration-catcher 的相关代码。项目线上地址是 https://inspiration.zzhhh.site，服务器通过 wsl -d Ubuntu-24.04 -- ssh cloud-op 登录。请基于当前 schema v4、多用户数据隔离、单灵感库归档和飞书异步镜像架构继续，不要重新实现已有功能，也不要在回复中输出任何密码、App Secret 或 API Key。我的新需求是：
```
